from flask import Flask, request
import os
import telebot
import time
from telebot.types import InlineKeyboardMarkup, InlineKeyboardButton

BOT_TOKEN = os.environ.get('BOT_TOKEN')
bot = telebot.TeleBot(BOT_TOKEN)
app = Flask(__name__)

# === СУПЕР-ПРОСТАЯ PvP ===
active_matches = {}
match_counter = 1

@bot.message_handler(commands=['start'])
def start_command(message):
    bot.send_message(message.chat.id, "⚽ PvP ТЕСТ - Используй /test")

@bot.message_handler(commands=['test'])
def test_command(message):
    global match_counter
    
    # Создаем матч
    match_id = f"test_match_{match_counter}"
    match_counter += 1
    
    # Сохраняем матч
    active_matches[match_id] = {
        'challenger_id': message.chat.id,
        'opponent_id': message.chat.id, 
        'status': 'pending'
    }
    
    print(f"🔴 СОЗДАН МАТЧ: {match_id}")
    print(f"🔴 Всего матчей: {len(active_matches)}")
    print(f"🔴 ID матча в кнопке: {match_id}")
    
    # Показываем кнопки
    keyboard = InlineKeyboardMarkup()
    keyboard.add(
        InlineKeyboardButton("✅ ПРИНЯТЬ ТЕСТ", callback_data=f"accept_{match_id}"),
    )
    
    bot.send_message(message.chat.id, 
        f"ТЕСТОВЫЙ ВЫЗОВ\nID матча: {match_id}\nНажми кнопку:",
        reply_markup=keyboard)

@bot.callback_query_handler(func=lambda call: call.data.startswith('accept_'))
def handle_accept(call):
    match_id = call.data[7:]  # убираем "accept_"
    
    print(f"🟢 НАЖАТА КНОПКА: {match_id}")
    print(f"🟢 Ищем матч: {match_id}")
    print(f"🟢 Всего матчей: {len(active_matches)}")
    print(f"🟢 Все матчи: {list(active_matches.keys())}")
    
    # Ищем матч
    match = active_matches.get(match_id)
    
    if not match:
        print(f"🔴 МАТЧ НЕ НАЙДЕН: {match_id}")
        bot.answer_callback_query(call.id, f"Матч {match_id} не найден!")
        return
    
    print(f"🟢 МАТЧ НАЙДЕН: {match}")
    
    # Меняем статус
    match['status'] = 'accepted'
    
    # Отправляем результат
    bot.send_message(call.message.chat.id, f"✅ МАТЧ ПРИНЯТ! ID: {match_id}")
    bot.answer_callback_query(call.id, "Успех!")
    
    # Удаляем матч
    del active_matches[match_id]
    print(f"🟢 МАТЧ УДАЛЕН: {match_id}")

@bot.message_handler(commands=['debug'])
def debug_command(message):
    debug_text = f"🔧 ДЕБАГ:\nМатчей: {len(active_matches)}\n"
    for match_id, match in active_matches.items():
        debug_text += f"🎯 {match_id}: {match['status']}\n"
    
    bot.send_message(message.chat.id, debug_text)

# === ВЕБХУК ===
@app.route('/webhook', methods=['POST'])
def webhook():
    if request.headers.get('content-type') == 'application/json':
        json_string = request.get_data().decode('utf-8')
        update = telebot.types.Update.de_json(json_string)
        bot.process_new_updates([update])
    return 'OK', 200

@app.route('/')
def index():
    return '⚽ PvP Debug Bot'

if __name__ == '__main__':
    webhook_url = f"https://{os.environ.get('RENDER_SERVICE_NAME')}.onrender.com/webhook"
    bot.remove_webhook()
    bot.set_webhook(url=webhook_url)
    print(f"Webhook: {webhook_url}")
    
    from waitress import serve
    serve(app, host="0.0.0.0", port=5000)
